# 挣值管理（EVM）考典

> 整理日期：2026-04-21
> 适用考试：信息系统项目管理师（高级）
> 本文覆盖：公式体系 · 推导过程 · 考试技巧 · 计算案例

---

## 一、基本概念

挣值管理（Earned Value Management，EVM）是一套将**范围、进度、成本**三者整合在一起的绩效测量方法，核心思想是：

> 用"完成了多少价值的工作"来衡量项目状态，而不是简单比较"花了多少钱"。

### 三个基本参数

| 参数 | 英文全称 | 中文名 | 本质含义 |
|------|---------|--------|---------|
| **PV** | Planned Value | 计划价值 | 到目前为止，**计划**完成工作的预算成本 |
| **EV** | Earned Value | 挣值 | 到目前为止，**实际完成**工作的预算成本 |
| **AC** | Actual Cost | 实际成本 | 到目前为止，**实际花费**的成本 |
| **BAC** | Budget at Completion | 完工预算 | 项目**全部工作**的预算总成本（成本基准，不含管理储备） |

**三者关系直觉：**
- PV 是"计划花多少"——反映进度计划
- EV 是"干了多少价值的活"——是连接进度和成本的桥梁
- AC 是"实际花了多少"——反映真实支出

---

## 二、九个核心公式

### 第①组：4个偏差与绩效指数（判断当前状态）

| 公式 | 名称 | 判断标准 | 记忆口诀 |
|------|------|---------|---------|
| **SV = EV − PV** | 进度偏差（Schedule Variance） | >0 超前，<0 落后，=0 正常 | **S 对 P** |
| **CV = EV − AC** | 成本偏差（Cost Variance） | >0 节省，<0 超支，=0 正常 | **C 对 A** |
| **SPI = EV / PV** | 进度绩效指数（Schedule Performance Index） | >1 超前，<1 落后，=1 正常 | |
| **CPI = EV / AC** | 成本绩效指数（Cost Performance Index） | >1 节省，<1 超支，=1 正常 | |

> 口诀总结：**偏差都是 EV 减（SV=EV-PV，CV=EV-AC），绩效指数 EV 除（SPI=EV/PV，CPI=EV/AC）；结果>0或>1是好事，<0或<1是坏事。**

---

### 第②组：2个 ETC 公式（预测剩余成本，二选一）

ETC（Estimate to Complete）= 从现在到项目完工**还需要花费**的成本。

| 假设类型 | 适用场景（题目关键词） | 公式 |
|---------|-------------------|------|
| **非典型偏差**（偶然偏差） | "剩余工作按原计划执行"、"按预算执行"、"偏差是偶然的" | **ETC = BAC − EV** |
| **典型偏差**（系统性偏差） | "按当前效率持续"、"按当前绩效推算"、"偏差会持续" | **ETC = (BAC − EV) / CPI** |

---

### 第③组：3个预测公式（预测最终结果）

| 公式 | 名称 | 说明 |
|------|------|------|
| **EAC = AC + ETC** | 完工估算（Estimate at Completion） | 通用公式，代入不同 ETC 得到不同展开形式 |
| **VAC = BAC − EAC** | 完工偏差（Variance at Completion） | >0 预测节省，<0 预测超支 |
| **TCPI = (BAC − EV) / (BAC − AC)** | 完工绩效指数（To-Complete Performance Index） | 要按 BAC 完工，剩余工作需达到的效率；>1 说明需要比当前更努力 |

---

## 三、公式推导

### 推导一：EAC 两种展开形式的等价性

**非典型偏差展开（直接代入）：**

```
EAC = AC + ETC
    = AC + (BAC − EV)        ← 代入 ETC = BAC − EV
```

这一步没有化简，就是直接代入，无需推导。

**为什么非典型偏差下 ETC = BAC − EV？**

EV 是"已完成工作的预算价值"，所以：

```
剩余工作的预算价值 = BAC − EV
```

非典型偏差假设"剩余工作按原计划预算执行"，即剩余工作花的钱 = 剩余工作的预算，因此：

```
ETC = BAC − EV
```

这是假设条件直接决定的，不是凑出来的。

---

**典型偏差展开（需要一步化简）：**

```
EAC = AC + ETC
    = AC + (BAC − EV) / CPI        ← 代入 ETC = (BAC−EV)/CPI
```

进一步化简，将 AC 用 CPI 表达：

```
由 CPI = EV / AC，得 AC = EV / CPI

EAC = EV/CPI + (BAC − EV)/CPI     ← 将 AC 替换为 EV/CPI
    = [EV + (BAC − EV)] / CPI      ← 通分，分母相同合并分子
    = BAC / CPI                     ← EV 与 −EV 抵消
```

**结论：** `EAC = AC + (BAC−EV)/CPI = BAC/CPI`，两者完全等价，只是写法不同。

---

### 推导二：TCPI 的含义

TCPI 的分子是"剩余工作量"，分母是"剩余预算"：

```
TCPI = 剩余工作量 / 剩余预算
     = (BAC − EV) / (BAC − AC)
```

- BAC − EV：还有多少价值的工作没做（剩余工作）
- BAC − AC：还有多少预算可以用（剩余预算）

TCPI > 1，说明剩余预算不够用，需要比当前 CPI 更高的效率才能完成目标。

---

## 四、考试技巧

### 技巧一：做题四步法

```
第一步：识别参数
        题目给了哪些数？BAC、完成%、AC？
        → PV = 计划完成% × BAC
        → EV = 实际完成% × BAC
        → AC = 题目直接给出

第二步：算偏差和绩效
        SV = EV − PV，CV = EV − AC
        SPI = EV/PV，CPI = EV/AC

第三步：判断典型 or 非典型
        题目说"按原计划/预算执行剩余工作" → 非典型 → ETC = BAC − EV
        题目说"按当前效率/趋势持续"       → 典型   → ETC = (BAC−EV)/CPI
        题目直接给出 EAC                  → 反推   → ETC = EAC − AC

第四步：算预测值
        EAC = AC + ETC（或 BAC/CPI 快捷算）
        VAC = BAC − EAC
        TCPI = (BAC−EV)/(BAC−AC)
```

---

### 技巧二：先算 EAC 还是先算 ETC？

| 情况 | 推荐顺序 | 原因 |
|------|---------|------|
| 典型偏差，已知 CPI | 先算 EAC = BAC/CPI，再反推 ETC = EAC−AC | 一步到位，更快 |
| 非典型偏差 | 必须先算 ETC = BAC−EV，再算 EAC = AC+ETC | 没有 EAC 的快捷公式 |
| 题目直接给出 EAC | 直接反推 ETC = EAC−AC | 无需判断典型/非典型 |

---

### 技巧三：高频陷阱

**陷阱1：EV 和 AC 混淆**
- EV = 实际完成工作的**预算**价值（干了多少价值的活）
- AC = 实际**花费**的成本（真实支出了多少钱）
- 两者都和"实际完成"有关，但一个是预算口径，一个是实际口径

**陷阱2：ETC 只有一个公式**
- 错误认知：ETC = (BAC−EV)/CPI
- 正确认知：ETC 有两种，必须先判断典型/非典型再选公式

**陷阱3：VAC 的公式**
- 错误写法：VAC = EAC − AC（这是 ETC 的公式）
- 正确写法：VAC = BAC − EAC

**陷阱4：BAC 的范围**
- EVM 中的 BAC 通常指**成本基准**，不含管理储备
- 项目总预算 = 成本基准（BAC）+ 管理储备
- 题目若给出"总预算"，需注意是否需要减去管理储备

**陷阱5：SV 和 CV 的名称**
- SV = Schedule Variance = **进度**偏差（不是成本）
- CV = Cost Variance = **成本**偏差（不是进度）
- 字母 S/C 对应的是偏差类型，不是公式中减的那个字母

---

### 技巧四：关键词速查表

| 题目出现的关键词 | 对应操作 |
|---------------|---------|
| 计划完成 X%，实际完成 Y% | PV = X%×BAC，EV = Y%×BAC |
| 剩余工作**按原计划**执行 | 非典型 → ETC = BAC−EV |
| 剩余工作**按预算**执行 | 非典型 → ETC = BAC−EV |
| 偏差是**偶然**的 | 非典型 → ETC = BAC−EV |
| 按**当前效率**持续 | 典型 → ETC = (BAC−EV)/CPI |
| 按**当前绩效**推算 | 典型 → ETC = (BAC−EV)/CPI |
| 偏差会**持续**到完工 | 典型 → ETC = (BAC−EV)/CPI |
| 要完成目标还需要多高效率 | TCPI = (BAC−EV)/(BAC−AC) |
| 预测最终超支/节省多少 | VAC = BAC−EAC |

---

## 五、计算案例

### 案例一：基础计算（非典型偏差）

**题目：** 某项目总预算 BAC = 200万元，目前计划完成 60%，实际完成 50%，实际花费 110万元。假设后续工作按原计划预算执行，求 SV、CV、SPI、CPI、ETC、EAC、VAC。

**解题过程：**

```
① 识别参数
   PV = 200 × 60% = 120万
   EV = 200 × 50% = 100万
   AC = 110万
   BAC = 200万

② 偏差与绩效
   SV = EV − PV = 100 − 120 = −20万   → 进度落后
   CV = EV − AC = 100 − 110 = −10万   → 成本超支
   SPI = EV/PV = 100/120 ≈ 0.83       → 进度落后
   CPI = EV/AC = 100/110 ≈ 0.91       → 成本超支

③ 判断类型："按原计划预算执行" → 非典型偏差

④ 预测
   ETC = BAC − EV = 200 − 100 = 100万
   EAC = AC + ETC = 110 + 100 = 210万
   VAC = BAC − EAC = 200 − 210 = −10万  → 预测超支10万
```

**结论：** 项目进度落后、成本超支，但认为超支是偶然的，剩余工作按预算执行，预测最终超支 10万元。

---

### 案例二：基础计算（典型偏差）

**题目：** 同上题条件（BAC=200万，PV=120万，EV=100万，AC=110万），但假设当前偏差会持续到项目结束，求 ETC、EAC、VAC、TCPI。

**解题过程：**

```
① 沿用案例一的参数：CPI ≈ 0.909

② 判断类型："偏差会持续" → 典型偏差

③ 预测
   ETC = (BAC − EV) / CPI = (200 − 100) / 0.909 ≈ 110万
   EAC = BAC / CPI = 200 / 0.909 ≈ 220万
   （验证：EAC = AC + ETC = 110 + 110 = 220万 ✓）
   VAC = BAC − EAC = 200 − 220 = −20万  → 预测超支20万

④ TCPI（若要按 BAC 完工）
   TCPI = (BAC − EV) / (BAC − AC) = (200−100) / (200−110) = 100/90 ≈ 1.11
   → 剩余工作需要达到 1.11 的效率，而当前 CPI 只有 0.91，难度很大
```

**结论：** 典型偏差下预测超支 20万（比非典型多 10万），且要完成目标需要大幅提效，风险较高。

---

### 案例三：对比两种假设的差异

**题目：** 某项目 BAC = 500万，当前 EV = 200万，AC = 220万，PV = 250万。分别按非典型和典型偏差计算 EAC，并说明差异。

```
基础参数：
   SV = 200 − 250 = −50万（进度落后）
   CV = 200 − 220 = −20万（成本超支）
   CPI = 200/220 ≈ 0.909

非典型偏差（认为超支偶然）：
   ETC = BAC − EV = 500 − 200 = 300万
   EAC = AC + ETC = 220 + 300 = 520万
   VAC = 500 − 520 = −20万

典型偏差（认为超支持续）：
   ETC = (BAC − EV) / CPI = 300 / 0.909 ≈ 330万
   EAC = BAC / CPI = 500 / 0.909 ≈ 550万
   VAC = 500 − 550 = −50万
```

| 指标 | 非典型偏差 | 典型偏差 |
|------|---------|---------|
| ETC | 300万 | 330万 |
| EAC | 520万 | 550万 |
| VAC | −20万 | −50万 |

**结论：** 典型偏差的预测更悲观。当 CPI < 1（成本超支）时，典型偏差的 EAC 总是大于非典型偏差的 EAC。

---

### 案例四：已知 EAC 反推 ETC

**题目：** 某项目 BAC = 400万，当前 AC = 180万，EV = 160万，PV = 200万。项目经理预测完工总成本 EAC = 430万，求 ETC、VAC、TCPI。

```
ETC = EAC − AC = 430 − 180 = 250万
VAC = BAC − EAC = 400 − 430 = −30万（预测超支30万）
TCPI = (BAC − EV) / (BAC − AC) = (400−160) / (400−180) = 240/220 ≈ 1.09
```

**结论：** 还需花费 250万，预测超支 30万，要完成目标需要将效率提升至 1.09（当前 CPI = 160/180 ≈ 0.89，差距较大）。

---

### 案例五：综合题（含 BAC 构成）

**题目：** 某项目总预算 300万（含管理储备 20万），应急储备 30万。当前计划完成 50%，实际完成 45%，实际花费 190万，CPI 按当前绩效持续。求：① 成本基准；② EAC；③ VAC。

```
① 成本基准（BAC）= 总预算 − 管理储备 = 300 − 20 = 280万
   （注意：EVM 中 BAC = 成本基准，不含管理储备）

② PV = 280 × 50% = 140万
   EV = 280 × 45% = 126万
   AC = 190万
   CPI = EV/AC = 126/190 ≈ 0.663

   典型偏差（按当前绩效持续）：
   EAC = BAC/CPI = 280/0.663 ≈ 422万

③ VAC = BAC − EAC = 280 − 422 = −142万（预测严重超支）
```

**注意：** 应急储备已包含在成本基准（BAC）内，不需要单独处理；管理储备不在 BAC 内，EVM 计算不涉及。

---

## 六、公式速查卡

```
┌─────────────────────────────────────────────────────────┐
│                   EVM 九公式速查                         │
├─────────────────────────────────────────────────────────┤
│ 参数：PV = 计划完成% × BAC                               │
│       EV = 实际完成% × BAC                               │
│       AC = 实际花费（题目直接给）                         │
├─────────────────────────────────────────────────────────┤
│ ① 偏差/绩效（判断现状）                                  │
│   SV = EV − PV    >0超前 <0落后                         │
│   CV = EV − AC    >0节省 <0超支                         │
│   SPI = EV / PV   >1超前 <1落后                         │
│   CPI = EV / AC   >1节省 <1超支                         │
├─────────────────────────────────────────────────────────┤
│ ② ETC（二选一）                                          │
│   非典型（偶然/按原计划）：ETC = BAC − EV                │
│   典型  （持续/按当前效率）：ETC = (BAC−EV) / CPI        │
│   已知EAC时反推：ETC = EAC − AC                         │
├─────────────────────────────────────────────────────────┤
│ ③ 预测（最终结果）                                       │
│   EAC = AC + ETC                                        │
│       → 非典型展开：AC + (BAC−EV)                       │
│       → 典型展开：  BAC / CPI                           │
│   VAC  = BAC − EAC   >0节省 <0超支                      │
│   TCPI = (BAC−EV) / (BAC−AC)   >1需提效                │
└─────────────────────────────────────────────────────────┘
```
